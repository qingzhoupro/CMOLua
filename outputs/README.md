# outputs 目录说明

此目录存放用户通过 Skill 生成的成果文件。

## 目录结构

```
outputs/
├── lua/          # Lua 脚本
├── config/       # 配置文件
└── docs/         # 说明文档
```

## 使用规则

1. 所有生成的文件必须保存到此目录
2. 文件命名格式：`[场景名]_[日期]_[描述].扩展名`
3. 保持目录整洁，不要散落文件
